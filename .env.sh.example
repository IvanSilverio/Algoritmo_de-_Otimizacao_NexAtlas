export NEXATLAS_DB_HOST=jetstream.nexatlas.com
export NEXATLAS_DB_PORT=5433
export NEXATLAS_DB_NAME=jetstream
export NEXATLAS_DB_USER=
export NEXATLAS_DB_PASSWORD=
# Opcional — default prefer (tenta SSL, usa se o servidor tiver; sem-SSL caso
# contrário). Setar require só quando o servidor falar SSL.
export NEXATLAS_DB_SSLMODE=prefer
